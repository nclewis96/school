$(document).ready(equalize);

var minHeight = 600;

/** 
* Ensures that the height of navigation and content panes are equal
*/
function equalize() 
{
	var highest = $('#form').height();
	
	if(highest < minHeight)
		highest = minHeight;
	
	$('.equal').height(highest);
}