
import { toLowercase } from 'ember-quickstart/helpers/to-lowercase';
import { module, test } from 'qunit';

module('Unit | Helper | to lowercase');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toLowercase([42]);
  assert.ok(result);
});

